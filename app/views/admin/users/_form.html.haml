= form_for [:admin, @user] do |f|
	= f.error_messages
	%p
		= f.label :email
		= f.text_field :email
	%p
		= f.label :password
		= f.password_field :password
	%p
		= f.check_box :admin
		= f.label :admin, "Is an admin?"
	= f.submit